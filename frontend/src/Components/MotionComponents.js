import Box from "@mui/material/Box";
import Button from "@mui/material/Button";
import { motion } from "motion/react";

export const MyButton = motion.create(Button);
export const MyBox = motion.create(Box);
